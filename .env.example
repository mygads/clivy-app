# ===========================================
# Genfity Backend Configuration
# ===========================================

JWT_SECRET=""
NEXT_PUBLIC_APP_URL="http://localhost:8090"
NODE_ENV="production"
PORT=8090
BLOB_READ_WRITE_TOKEN=""

# Database Configuration
DB_USER=
DB_PASSWORD=
DB_NAME=
DB_HOST=
DB_PORT=5432

# Database URL for Prisma
# For local development (with variable expansion):
# DATABASE_URL="postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}"
# For production/Vercel (direct URL without variable expansion):
DATABASE_URL="postgresql://username:password@host:port/database?sslmode=require"

# Email server configuration
EMAIL_SERVER_HOST="" 
EMAIL_SERVER_PORT="465" 
EMAIL_SERVER_USER=""
EMAIL_SERVER_PASSWORD=""

WHATSAPP_SERVER_API=""
WHATSAPP_ADMIN_TOKEN=""
WHATSAPP_USER_TOKEN=""

# Cron Security
CRON_API_KEY=""
CRON_SECRET=""

# DigitalOcean API Token for server monitoring (example placeholder)
DIGITALOCEAN_TOKEN=""
